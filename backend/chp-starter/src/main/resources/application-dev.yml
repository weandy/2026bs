# 开发环境配置
spring:
  datasource:
    # 居民域数据源
    resident:
      jdbc-url: jdbc:mysql://127.0.0.1:3306/chp_resident?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
      username: root
      password: root123@
      driver-class-name: com.mysql.cj.jdbc.Driver
      hikari:
        pool-name: ResidentPool
        maximum-pool-size: 10
        minimum-idle: 5
        connection-timeout: 30000

    # 管理域数据源
    admin:
      jdbc-url: jdbc:mysql://127.0.0.1:3306/chp_admin?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
      username: root
      password: root123@
      driver-class-name: com.mysql.cj.jdbc.Driver
      hikari:
        pool-name: AdminPool
        maximum-pool-size: 10
        minimum-idle: 5
        connection-timeout: 30000

# ── 本地开发 JWT 明确配置（覆盖 application.yml 中的占位符）──
jwt:
  secret: chp-community-health-platform-jwt-secret-key-2024-dev
  access-token-expiration: 7200000
  refresh-token-expiration: 604800000
